# === Dependencias base (Streamlit Cloud compatible) ===

# Cliente LLM (compatible con LM Studio y Ollama)
openai>=1.10.0

# Utilidades
pandas>=2.1.0
openpyxl>=3.1.0
python-dotenv>=1.0.0

# UI de agentes
streamlit>=1.34.0

# Graficas interactivas y exportacion PDF
plotly>=5.18.0
kaleido>=0.2.1

# === Dependencias opcionales (instalar local para audio/GPU) ===
# pip install -r requirements-local.txt
