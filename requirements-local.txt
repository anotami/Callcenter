# Dependencias completas para instalacion local (audio, GPU, BD)
# Incluye todo lo necesario para transcripcion y diarizacion
#
# Instalar: pip install -r requirements-local.txt

# Base (ya incluidas en requirements.txt)
openai>=1.10.0
pandas>=2.1.0
python-dotenv>=1.0.0
streamlit>=1.34.0

# Transcripcion (faster-whisper es ~4x mas rapido que whisper original)
faster-whisper>=1.0.0

# Diarizacion
pyannote.audio>=3.1
huggingface_hub>=0.20.0

# Motor IA / GPU
torch>=2.1.0
torchaudio>=2.1.0

# Base de datos SQL Server
pyodbc>=5.0
sqlalchemy>=2.0

# Procesamiento de audio
pydub>=0.25.1
